{
  "name": "font-changer-replacer",
  "version": "1.0.0",
  "description": "Figma plugin to scan, swap, and apply text styles across Selection, Page, or Document",
  "scripts": {
    "test": "npm run tsc && npm run build",
    "format": "prettier --write .",
    "tsc": "npm run tsc:main && npm run tsc:ui",
    "tsc:main": "tsc --noEmit -p plugin-src",
    "tsc:ui": "tsc --noEmit -p ui-src",
    "tsc:watch": "concurrently -n widget,iframe \"npm run tsc:main -- --watch --preserveWatchOutput\" \"npm run tsc:ui -- --watch --preserveWatchOutput\"",
    "build": "npm run build:ui && npm run fix-html && npm run build:main",
    "fix-html": "node -e \"const fs=require('fs');const h=fs.readFileSync('dist/index.html','utf8');fs.writeFileSync('dist/index.html',h.replace('type=\\\"module\\\" crossorigin',''))\"",
    "build:main": "esbuild plugin-src/code.ts --bundle --outfile=dist/code.js --target=es2017 --format=iife",
    "build:ui": "npx vite build --minify esbuild --emptyOutDir=false",
    "build:watch": "concurrently -n widget,iframe \"npm run build:main -- --watch\" \"npm run build:ui -- --watch\"",
    "dev": "concurrently -n tsc,build 'npm:tsc:watch' 'npm:build:watch'"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@figma/plugin-typings": "*",
    "@types/react": "^18.2.38",
    "@types/react-dom": "^18.2.16",
    "@vitejs/plugin-react-refresh": "^1.3.6",
    "concurrently": "^8.2.2",
    "esbuild": "^0.19.7",
    "prettier": "^3.1.0",
    "typescript": "^5.3.2",
    "vite": "^5.0.0",
    "vite-plugin-singlefile": "^0.13.5"
  }
}

